import React from "react";
import styles from "./login_footer.module.css";

const Login_footer = (props) => (
  <header className={styles.footer}>
    <h1>Make Your Self</h1>
  </header>
);

export default Login_footer;
