import React from "react";
import styles from "./login_header.module.css";

const Login_header = (props) => (
  <header className={styles.header}>
    <h1>Mobile invitation Maker</h1>
  </header>
);

export default Login_header;
