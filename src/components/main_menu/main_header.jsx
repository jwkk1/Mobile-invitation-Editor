import React from "react";
import styles from "./main_header.module.css";

const Main_header = (props) => (
  <header className={styles.header}>
    <h1 className={styles.title}>Mobile invitation Maker</h1>
  </header>
);

export default Main_header;
